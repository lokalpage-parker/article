spring:
  application:
    name: article
#  jpa:
#    open-in-view: true
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

